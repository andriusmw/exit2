import { Auth } from "./Auth";

export const Header = () => {
    return (
        <header>
            <h1>HEADER</h1>
            <nav>
                <Auth />
            </nav>
        </header>
    );
};
